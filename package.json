{
  "name": "aoc-gifter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf docs",
    "build": "npm run clean && node scripts/build.js",
    "dev": "npm run build && live-server docs --port=8000 --open=./index.html",
    "start": "npm run build && live-server public --port=8000 --open=./index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
